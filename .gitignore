# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Node / frontend
node_modules/
npm-debug.log*
yarn-error.log*
# NOTE: frontend/dist is intentionally committed so hosts can serve the app
# without a Node build step. Rebuild with `npm run build` and recommit if you
# change the frontend.

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Scratch
*.pdf
!docs/**/*.pdf
